#!/bin/bash

function ff() {
    name="$@"

    if [ -z "$name" ]; then
        echo "Usage: ff <name>"
        return
    fi

    find . -iname "*$name*"
}

function ffd() {
    name="$@"

    if [ -z "$name" ]; then
        echo "Usage: ffd <name>"
        return
    fi

    find . -iname "*$name*" -type d
}

function ffx() {
    name="$@"

    if [ -z "$name" ]; then
        echo "Usage: ffd <name>"
        return
    fi

    find . -iname "*$name*" -type f -perm +111
}
