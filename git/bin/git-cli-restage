#!/usr/bin/env perl

use strict;
use warnings;
use CLI;
use CLI::Git;

my $options = CLI::options();
my $index = shift @ARGV || "HEAD^1";

CLI::usage("[tree-ish]") if $options->{"help"};

CLI::run(["git", "reset", "--soft", $index]);
