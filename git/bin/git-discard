#!/bin/bash

if [ $# -eq 0 ]; then
    git reset --hard
else
    if [ "$1" = "origin" ]; then
        git reset --hard origin
    else
        for i in "$@"; do
            git checkout -- "$i"
        done
    fi
fi
