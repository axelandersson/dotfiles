#!/usr/bin/env perl

use strict;
use warnings;
use CLI;
use CLI::Git;

my $options = CLI::options();
my $branch = shift @ARGV;

CLI::usage("") if $options->{"help"};

CLI::run(["git", "remote", "-v"]);
