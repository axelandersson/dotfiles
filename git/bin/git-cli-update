#!/usr/bin/env perl

use strict;
use warnings;
use CLI;
use CLI::Git;

my $options = CLI::options();

CLI::usage() if $options->{"help"};

CLI::run(["git", "pull", "--rebase", "--autostash", "--all"]);
