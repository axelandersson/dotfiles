#!/usr/bin/env perl

use strict;
use warnings;
use CLI;
use CLI::Git;

if($^O eq "darwin" && 0) {
    CLI::run(["git", "credential-osxkeychain", @ARGV]);
} else {
    CLI::run(["git", "credential-store", @ARGV]);
}
