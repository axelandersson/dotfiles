#!/usr/bin/perl -w

use strict;
use CLI;

my $options = CLI::options();

CLI::usage() if $options->{"help"};

if(CLI::can_run("git-up")) {
    CLI::run("git up");
} else {
    CLI::run("git pull --rebase --all");
}
